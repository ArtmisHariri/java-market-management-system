public class UI {

        String message;
        boolean done;

        public UI(String message, boolean done) {
            this.message = message;
            this.done = done;
        }

        public String getMessage() {
            return message;
        }

        public boolean isDone() {
            return done;
        }

    }

